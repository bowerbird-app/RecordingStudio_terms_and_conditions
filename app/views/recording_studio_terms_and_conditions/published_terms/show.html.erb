<% if respond_to?(:recording_studio_page_nav) %>
  <% recording_studio_page_nav(title: @terms&.title.presence || "Terms") %>
<% end %>

<%= render FlatPack::PageTitle::Component.new(
  title: @terms&.title.presence || "Terms and Conditions",
  subtitle: terms_heading_date(@terms),
  variant: :h1
) %>

<%= terms_content(@terms&.body) %>
